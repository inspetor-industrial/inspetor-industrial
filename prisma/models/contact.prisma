model Contact {
  id          String   @id @default(cuid())
  name        String
  email       String
  phone       String
  service     String
  description String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
